# Copy this file to docket/.local/docket-runtime.env (gitignored) and fill it.
# This file BEATS system env vars - the VS Code extension host inherits env
# from VS Code launch time, which is stale after you export in a terminal.
JIRA_BASE_URL=https://jira.example.com
JIRA_PAT=your-personal-access-token
