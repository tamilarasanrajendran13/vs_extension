# Project context - <project-name>

Copy this file to context/<project-name>.md. The pipeline injects it into
the comprehension and planning stages; missing context is a warning, never a
halt. Keep it under a page - it is prompt payload.

## What this project is
One paragraph: what the system does, for whom.

## Architecture in five lines
The major modules/dirs and what owns what.

## Conventions that matter
Test layout, naming rules, patterns a developer must follow.

## Danger zones
Files or behaviors a change must not touch without a human.
