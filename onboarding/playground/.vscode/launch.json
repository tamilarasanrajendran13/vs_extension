{
  "_comment": "Makes F5 deterministic. Without this file VS Code asks you to pick a debugger; with it, F5 goes straight to the Extension Development Host.",
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run the Docket Playground",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}
